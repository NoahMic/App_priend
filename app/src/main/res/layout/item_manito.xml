<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="52dp">

        <androidx.cardview.widget.CardView
            android:id="@+id/manito_img"
            android:layout_width="42dp"
            android:layout_height="42dp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:cardCornerRadius="42dp"
            android:background="#797878"
            android:layout_marginBottom="10dp" >
            <ImageView
                android:layout_width="match_parent"
                android:background="#797878"
                android:layout_height="match_parent" />
        </androidx.cardview.widget.CardView>

        <TextView
            android:id="@+id/member_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toRightOf="@+id/manito_img"
            android:textSize="20sp"
            android:textColor="#2bd647"
            android:fontFamily="@font/font3"
            android:text="친구1"
            app:layout_constraintTop_toTopOf="@id/manito_img"
            app:layout_constraintBottom_toBottomOf="@id/manito_img"
            android:layout_marginLeft="12dp"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>